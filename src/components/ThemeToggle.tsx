import React from 'react';

// This component is no longer needed since we're light mode only
const ThemeToggle: React.FC<{ className?: string }> = () => {
  return null;
};

export default ThemeToggle;